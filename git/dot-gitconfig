[user]
	name = Evan Torrie
	useConfigOnly = true
	email = evant@oath.com
	signingkey = CB736BC2
[init]
	templatedir = ~/.git/template
[pull]
	rebase = true

[include]
         path = ~/.git/evant_at_oath.inc
#        path = ~/.git/include.inc

; include for all repositories inside $HOME/gitrepos/git.ouroath
[includeIf "gitdir:~/gitrepos/work/"]
	path = ~/.git/evant_at_oath.inc

; include for all repositories inside $HOME/gitrepos/github.com
[includeIf "gitdir:~/gitrepos/ext/"]
	path = ~/.git/etorrie_at_gmail.inc

[rebase]
	autoStash = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[diff]
	submodule = diff
[alias]
	subpull = !git pull && git submodule sync --recursive && git submodule update --init --recursive
[core]
	abbrev = 5
[gpg]
	program = gpg
